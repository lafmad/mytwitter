#coding: utf-8
<% provide(:title, 'Sign up') %>
<h1></h1>

<div class="row">
	<div class="span4 offset4">
		<%= form_for(@user) do |f| %>
		<%= render 'fields',f: f %>
		<%= f.submit "注册", class: "btn btn-large btn-primary" %>
		<% end %>
	</div>
</div>